name: Verify

on:
  workflow_call:
    inputs:
      ref:
        description: Exact commit to verify for a caller.
        required: true
        type: string
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    name: Verify application
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out source
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          ref: ${{ inputs.ref || github.sha }}
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7.6.0
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-groups --locked

      - name: Run all checks
        run: |
          bash -n deploy/deploy-main
          uv run ruff check .
          uv run basedpyright
          uv run python -m unittest discover -s tests -v
          uv run apasz-hub-email-test --help
